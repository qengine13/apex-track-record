"""Self-contained, zero-dependency chain verifier — vendored from the
private trade repo (intelligence_core/track_record) on 2026-06-12 so the
public record verifies with no external checkout:
    python -m verifier [chain_dir]
"""
